<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Resume summary</title>
    <%- include("shared/links"); %>
    <style>
        body {  
            /* background: #121212; */
            color: white;
            font-family: 'Segoe UI', sans-serif;
        }
        .navbar {
            background: linear-gradient(to right, #0062E6, #33AEFF);
        }
        .navbar-brand {
            font-weight: 700;
            font-size: 28px;
            letter-spacing: 1px;
        }
        .btn{
            border-radius: 12px;
            padding: 10px 23px;
            font-weight: 600;
        }
        #logoutButton {
            background-color: #dc3545;
            color: white;
            border: none;
        }
        .summary{
            border: 2px solid cadetblue;
            border-radius: 24px;   
        }
    </style>
</head>
<body>
    <!-- Header -->
    <%- include("shared/header", {loggedin : isAuth}); %>

    <!-- Toast -->
    <div class="toast-container position-fixed bottom-0 end-0 p-3">
        <div class="toast hide" id="addToast" role="alert" aria-live="assertive" aria-atomic="true">
            <div class="toast-header">
                <img src="/images/detail.png" class="rounded me-2" alt="..." width="40" height="40">
                <strong class="me-auto">AI resume</strong>
                <small class="text-body-secondary">just now</small>
                <button type="button" class="btn-close" data-bs-dismiss="toast" aria-label="Close"></button>
            </div>
            <div class="toast-body text-black">
                Resume Summary added in DB
            </div>
        </div>
    </div>

    <div class="container">
        <div class="summary mt-5">
            <div class="summary text-black p-4">
                <p><%= summary %></p>
            </div>
        </div>

        <div class="d-flex gap-3 mt-5">
            <button type="button" class="btn btn-secondary" onclick="gotoPreviewPage()">Preview page</button>
            <button type="button" class="btn btn-warning" onclick="gotoSkillsPage()">Previous page</button>
            <button type="button" class="btn btn-info" onclick="regenerate()">Re-genereate</button>
        </div>
    </div>
   
    <script src="/js/summary.js"></script>
</body>
</html>
