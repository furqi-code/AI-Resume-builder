<nav class="navbar navbar-expand-lg shadow-sm py-3">
    <div class="container">
        <a class="navbar-brand text-white" href="#">Resume Builder</a>
        <div class="ms-auto">
            <% if(loggedin){ %>
                <button class="btn btn-light btn-custom" id="logoutButton" onclick="logout()">Log out</button>
            <% }else{ %>
                <button class="btn btn-outline-light me-2 btn-custom" data-bs-toggle="modal" data-bs-target="#loginModal" id="loginButton">Sign In</button>
                <button class="btn btn-light text-primary me-2 btn-custom" data-bs-toggle="modal" data-bs-target="#signupModal" id="signupButton">Sign Up</button>
            <% } %>
        </div>
    </div>
</nav>